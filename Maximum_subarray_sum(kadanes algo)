class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        maximum_sum = nums[0]
        arr_sum = 0
        for i in nums:
            arr_sum += i
            if arr_sum > maximum_sum:
                maximum_sum = arr_sum
            if arr_sum < 0:
                arr_sum = 0
        return maximum_sum
